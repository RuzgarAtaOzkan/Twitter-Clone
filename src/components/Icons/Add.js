// MODULES
import React from 'react';
import { IoIosAddCircleOutline } from 'react-icons/io';

function Add({ active }) {
  return active ? (
    <IoIosAddCircleOutline />
  ) : (
    <IoIosAddCircleOutline />
  );
}

export default Add;
