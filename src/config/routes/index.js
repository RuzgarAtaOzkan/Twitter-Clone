const config = {
  endpoints: {
    root: '/',
    home: '/home',
    signin: '/signin',
    signup: '/signup',
    explore: '/explore',
    notifications: '/notifications',
    messages: '/messages',
  },
};

export default config;
